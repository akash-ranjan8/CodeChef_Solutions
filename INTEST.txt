#include<stdio.h>
int main()
{
    int n,k,c,i,m;
    c=0;
    scanf("%d%d",&n,&k);
    for(i=0;i<n;++i)
    {
        scanf("%d",&m);
        if(m%k==0)
        {
            c++;
        }
    }
    printf("%d",c);
    return 0;
}

